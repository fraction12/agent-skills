interface:
  display_name: "/keepcoding"
  short_description: "Run parallel spec-driven coding waves"
  default_prompt: "Use $keepcoding (/keepcoding) to build my pre-written spec or OpenSpec change through parallel vertical slices."
